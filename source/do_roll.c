#include "../includes/push_swap.h"

void	do_roll(t_list **lst, int step)
{
	t_list	*tmp;
	int	list_size;

	if (!lst || ft_lstsize(*lst) <= 1)
		return ;
	list_size = ft_lstsize(*lst);
	tmp = *lst;
	if ()
}
